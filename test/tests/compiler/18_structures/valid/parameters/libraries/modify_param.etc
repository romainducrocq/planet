#  Modify a parameter of structure type
#  * 

type struc inner(    d: f64    , i: i32    
    )

type struc outer(
    s: struc inner    
    , ptr: *struc inner    , l: i64    )

pub fn modify_simple_struct(s: struc inner) i32;
pub fn modify_nested_struct(s: struc outer) i32;
