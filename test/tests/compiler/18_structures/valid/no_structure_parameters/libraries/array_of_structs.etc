#  Test that we can pass a pointer to an array of structures as a parameter 

type struc inner(    l: i64    , arr: [2]char    ) #  size: 8 bytes, alignment: 4 bytes

type struc outer(    a: char#  byte 0
    
    , b: struc inner#  bytes 4-11

    ) #  size: 12 byte, alignment: 4 bytes

pub fn validate_struct_array(struct_array: *struc outer) i32;
