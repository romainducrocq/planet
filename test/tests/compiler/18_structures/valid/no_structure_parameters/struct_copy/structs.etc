








use `stdlib`
use `string`

type struc small(    a: i32    , b: i64    
    )

type struc s(    arr: [3]char    , inner: struc small    )

type struc with_end_padding(    a: i32    , b: i32    , c
    : char    
    )
