pub fn calloc(nmemb: u64, size: u64) *any;
pub fn malloc(size: u64) *any;

type union simple(    i: i32    , l: i64    
    , c: char    , uc_arr: [3]u8    )

type union has_union(
    d: f64    
    , u: union simple    , u_ptr: *union simple    
    )

type struc simple_struct(    l: i64    , d: f64    , u: u32    )

type union has_struct(    l: i64    
    , s: struc simple_struct    )

type struc struct_with_union(    u: union simple    , ul: u64    )

type union complex_union(    d_arr: [2]f64    
    , s: struc struct_with_union    , u_ptr: *union has_union    )
