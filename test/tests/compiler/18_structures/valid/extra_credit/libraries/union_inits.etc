







#  library functions
pub fn strcmp(s1: string, s2: *char) i32;

type union simple(    d: f64    , c: char    , ptr: *i32    )

type union inner(
    arr: [9]char    
    )

type struc my_struct(    l: i64    , u: union inner    
    , i: i32    
    )

type union nested(
    str: struc my_struct    , s: union simple    
    , l: i64    
    )

pub fn validate_simple(ptr: *union simple) i32;
pub fn validate_simple_converted(ptr: *union simple) i32;
pub fn validate_nested(ptr: *union nested) i32;
pub fn validate_nested_partial(ptr: *union nested) i32;

