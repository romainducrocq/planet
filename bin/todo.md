1. rename reserved keywords:
  > For now renamed by sed in transpile.sh
  - type (also affects get_type_fmt) -> ast_t, stype, type_t, ...?
  - match -> smatch, ...?
  - string (not in sds) -> str, literal, ...?
